{
  "apiVersion": "discobox.dev/image/v1",
  "env": {
    "DSH_TELEMETRY_MODE": "DISABLED"
  },
  "harness": {
    "id": "dsh",
    "name": "DeepSeek Harness",
    "description": "DeepSeek Harness browser UI configured for CLI Proxy API.",
    "secrets": [
      { "name": "CLI_PROXY_API_KEY", "required": true },
      { "name": "CF_ACCESS_CLIENT_ID", "required": true },
      { "name": "CF_ACCESS_CLIENT_SECRET", "required": true }
    ],
    "config": {
      "reminder": "Enter the CLI Proxy API and Cloudflare Access credentials, then choose default, judge, and fast models.",
      "command": ["/usr/local/libexec/discobox/configure-dsh"]
    }
  }
}
